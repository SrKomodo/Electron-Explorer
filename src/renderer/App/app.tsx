import React from "react";

import TitleBar from "../TitleBar/titlebar";

class App extends React.Component {
  constructor(props: {}) {
    super(props);
  }

  render() {
    return (
      <TitleBar/>
    );
  }
}

export default App;
